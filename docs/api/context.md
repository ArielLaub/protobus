# Moved: Context

> [!NOTE]
> This page now lives at **[`docs/reference/api/context.md`](../reference/api/context.md)**.

The documentation was reorganised into four sections — `guide/`, `concepts/`,
`reference/` and `operations/` — so that the pages an operator needs are no
longer filed under "advanced". This stub keeps existing links working; it will
be removed in the next major release.

- **[Context](../reference/api/context.md)** — the current page
- **[Documentation index](../README.md)** — everything else
